2013.6
      1 更改APM_Config.h
      2 八轴更改AP_MotorsOcta.cpp中机架配置
2013.8
      1 主机模式下 Control_modes.ino中的read_control_switch()中的模式切换代码
      2 主机AP_MotorsOcta.cpp中setup_motors()代码，用于模式切换
      3 更改AP_MotorsMatrix.cpp中的output_armed()及output_disarmed()函数代码；
        用于从机的控制（后恢复）
      4 HAL_AVR_APM2_Class.cpp中console由uartA改为uartB，因uartB连接GPS, 不使用。
        但uartB未初始化，会不会因此而出错？
      5 调试阶段将slave工程中system.ino文件Init_ardupilot()中gcs0的uartA改为
        uartB。暂时不可通过A串口与地面站连接，待调试完毕后，应改回；
        主机也如此。


注意事项:
1 现阶段从机不为arm状态时，亦可从主机收到电机的消息，但此时可能无法传送给地面站
  正确的电机输出(可能根本不传该数据);1 



问题：
1 Arduino编译器可能有些问题，AP_MotorsMatrix.cpp中头文件名称不对居然能编译通过；
  在master工程中，头文件规定为slave模式，含未定义的变量slaveMode，居然有一次也
　能编译通过。

gcs_send_text_P()中gcs0.send_text_P(severity, str);

2 从机中若将fastloop屏蔽，红灯将不亮。后经调试发现：loop中一直进入
  if (ins.num_samples_available() >= 2)分支中，另一个分支条件不满足，故得不到执行。

3 从机中，console初始化为uartB，启动时串口仍有打印字符，而串口为UARTA，
  为什么？若如此，则主机初始化后可能发送很多字符给从机，超过9后，从机接受可能有问题


  有无可能C串口与gcs冲突的原因，usb与gcs的优先级

      

